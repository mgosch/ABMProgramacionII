@model IEnumerable<WebApplication1.Models.GamesGenres>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idGame)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idGenre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idGame)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idGenre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idGameGenre">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idGameGenre">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idGameGenre">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
